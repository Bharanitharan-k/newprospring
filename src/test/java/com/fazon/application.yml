spring:
  application:
    name: simplybyte-springboot
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/fazon_db_v1}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:admin123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8090

ingenico:
  api-key-id: ${INGENICO_API_KEY_ID:05ca0b5d914055ff}
  api-secret-key: ${INGENICO_API_SECRET_KEY:gD6r8NoqVY3yxsD3FT41uUTX2UXXqXJvXpALD/3pdo0=}
  uri: ${INGENICO_URI:https://api.preprod.connect.worldline-solutions.com/}
